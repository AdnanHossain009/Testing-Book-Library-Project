<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestingBookLibraryProject"
        tools:targetApi="31">

        <activity
            android:name=".Activity.Website_Activity"
            android:exported="false" />

        <activity
            android:name=".Activity.Favourite_Books_Activity"
            android:exported="false"
            android:label="@string/activity_favourite_books"/>

        <activity
            android:name=".Activity.Currently_Reading_Books_Activity"
            android:exported="false"
            android:label="@string/activity_currently_reading_books"/>

        <activity
            android:name=".Activity.Want_To_Read_Activity"
            android:exported="false"
            android:label="@string/activity_want_to_read"/>

        <activity
            android:name=".Activity.Already_Read_Book_Activity"
            android:exported="false"
            android:label="@string/activity_already_read_book"/>

        <activity
            android:name=".Activity.Book_Activity"
            android:exported="false"
            android:label="@string/activity_book"/>

        <activity
            android:name=".Activity.All_Books_Activity"
            android:exported="false"
            android:label="@string/activity_all_books"/>

        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>